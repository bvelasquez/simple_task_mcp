# Task Completion Checklist

1. Verify `projects.json` contains necessary project entries and keys.
2. Run `npm run build` to ensure TypeScript compiles.
3. Optionally run `npm run dev` to test interactions via MCP client.
4. Update README/tool documentation if new tools or endpoints added.
5. No automated tests; consider manual tool invocation for verification.
